name: scope_example
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  scope_manager:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  # rules
  mjk_lint_rules: ^4.0.3

flutter:
  uses-material-design: true
